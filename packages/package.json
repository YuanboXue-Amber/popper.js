{
  "name": "root",
  "private": true,
  "scripts": {
    "maxSize": "MODIFIER=maxSize PACKAGE=popper-max-size-modifier rollup -c && PACKAGE=popper-max-size-modifier node replace-import"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.8.7",
    "@babel/preset-flow": "^7.8.3",
    "rollup": "^2.1.0",
    "rollup-plugin-babel": "^4.4.0"
  }
}
